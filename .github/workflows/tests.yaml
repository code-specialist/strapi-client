name: Tests

on: [push]

jobs:
  test:
    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v2

    - name: Install dependencies
      run: |
        sudo npm install -g pnpm
        pnpm install

    - name: Run tests
      run: |
        pnpm run test
